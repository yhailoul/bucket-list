{% extends 'base.html.twig' %}

{% block extrastyle %}

{% endblock %}

{% block body %}

    <div class="content">
        <h2>{{ category.name }}</h2>
        <section>
            {% for wish in wishes %}
                <article>
                    <a href="{{ path('wish_list_details',{'id':wish.id}) }}"><p>{{ loop.index }} : {{ wish.title }} </p></a>
                </article>
            {% endfor %}
        </section>
    </div>

{% endblock %}

